**Prompt_ID: reponame 1.3 create_readme.md**

Task:
Create or rewrite the root `README.md` by converting the project idea in `docs/setup/short_description.md` into a proper README, following the repository’s README guidance document located at `/readme.md`.

Instructions:
1. Read:
   - `docs/setup/short_description.md`
   - `/readme.md` (this is the README guidance document to follow)

2. Produce a clean, user-facing `README.md` for the project.

Requirements:
- Use the short description as the source of truth for the project idea.
- Follow the structure, expectations, and writing rules defined in `/readme.md`.
- The README should describe the project from the perspective of a user, operator, or developer encountering the repo for the first time.
- Make the README clear, practical, and easy to scan.
- Preserve alignment with the short description.
- Do not invent major capabilities, architecture, workflows, or implementation details that are not supported by the short description.
- Do not turn the README into a deep design document.
- Do not include speculative future features unless the guidance explicitly calls for them.
- Keep the README grounded in the current intended purpose of the project.

What the README should achieve:
- explain what the project is
- explain what it broadly does
- explain why it exists
- make clear who it is for or how it is used, if that is inferable from the short description
- provide an appropriate getting-started or usage-oriented framing if the README guidance expects it

Output requirements:
- Write the final content directly into `README.md`
- Ensure the README reads naturally and professionally
- Prefer clarity over verbosity
- If the short description is ambiguous, make the most conservative reasonable interpretation and note assumptions briefly at the end

At the end of your response, include:
1. A short summary of what you changed
2. Any assumptions you had to make
3. Any mismatches or missing information between `docs/setup/short_description.md` and `/readme.md