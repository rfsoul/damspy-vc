**Prompt_ID: reponame 2.1 create_first_code pass.md**

You are implementing the first working code pass for this repository. 
Read: short description README implementation strategy technical guide/reference material if present 
Success criteria, in priority order: 
The code must be operationally correct for the core use case. 
The code must run from a fresh checkout without obvious startup/import/wiring failures. 
The implementation must stay within the intended scope, stack, and architecture direction. 
Simpler working code is preferred over cleaner but riskier architecture. 
It is acceptable for the first pass to be less featured or less polished, provided the core workflow works correctly. 

Rules: Do not redesign the system beyond what is needed to make it work. 
Do not add speculative features. 
Preserve domain terminology and known-good technical behavior. 
Prefer direct implementations where they improve first-pass reliability. 
Before finishing, check for obvious first-run problems: missing files, broken imports, missing static/template assets, inconsistent route wiring, and mismatched names.
