**Prompt_ID: reponame 1.4 create implementation_strategy.md**

Task:
Create or rewrite `docs/implementation/implementation_strategy.md` using the established project documents as source material, and following the guidance contained in the existing `docs/implementation/implementation_strategy.md` file.

Instructions:
1. Read:
   - `docs/setup/short_description.md`
   - `README.md`
   - `docs/setup/project_definition.md`
   - `docs/implementation/implementation_strategy.md` (treat the existing contents as the guideline/template to follow)
   - all relevant guide documents in the `reference/` folder

2. Produce a completed `docs/implementation/implementation_strategy.md`.

Requirements:
- Treat `docs/setup/short_description.md` as the original source of truth for the project idea.
- Treat `README.md` as the user-facing interpretation of the project.
- Treat `docs/setup/project_definition.md` as the source of scope, boundaries, assumptions, and non-goals.
- Use relevant documents in `reference/` as implementation guidance, prior art, constraints, examples, or operational hints where applicable.
- Follow the structure, headings, expectations, and writing rules contained in the current `docs/implementation/implementation_strategy.md`.

The implementation strategy should:
- define how the project should be built in its initial implementation phase
- describe the intended technical approach
- describe the intended runtime structure
- define the first practical implementation shape
- describe the validation approach
- identify important constraints, tradeoffs, and forbidden shortcuts
- stay aligned with the short description, README, and project definition
- take useful guidance from `reference/` documents without letting them silently override the project’s actual stated purpose

Important constraints:
- Do not invent major capabilities not supported by the source documents.
- Do not drift into a vague wish-list or speculative future roadmap.
- Do not turn this into a full architecture document unless the guideline explicitly requires it.
- Do not copy reference material blindly; interpret it in the context of this project.
- Prefer a practical minimum working implementation if the project is substantial.
- Where appropriate, describe the first phase as a clean minimum working concept that can later be expanded.
- Keep the document concrete enough that coding can begin from it.

Use of `reference/`:
- Read all guide documents in `reference/` that appear relevant.
- Incorporate useful implementation direction, constraints, patterns, or lessons from them.
- If a reference guide conflicts with the short description, README, or project definition, prefer the project-specific docs and note the conflict briefly.

Output requirements:
- Write the final content directly into `docs/implementation/implementation_strategy.md`
- Make it clear, practical, and implementation-oriented
- Prefer explicit decisions over vague possibilities
- If source information is incomplete, make conservative assumptions only
- Keep the document suitable as the key implementation-planning document before coding begins

At the end of your response, include:
1. A short summary of what you changed
2. Any assumptions you had to make
3. Any missing, ambiguous, or conflicting information across:
   - `docs/setup/short_description.md`
   - `README.md`
   - `docs/setup/project_definition.md`
   - relevant `reference/` documents